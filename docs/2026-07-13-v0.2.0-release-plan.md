# gpt-connector v0.2.0 release計画

作成日: 2026-07-13

状態: 実行中

## 目的

正規file attachment、durable consult job、diagnostics、全regular file pass-throughを含む現行sourceを`gpt-connector@0.2.0`として公開し、GitHubとnpm、local global installを一致させる。

## version裁定

`0.1.0 → 0.2.0`。既存CLI／MCP契約を残した後方互換のある機能追加であり、minor releaseとする。

## TODO

- [x] branch、remote、working tree、npm公開version、npm認証を確認する。
- [x] README、公開contract、RAG、INDEX、release記録の現行記述を`0.2.0`へ揃える。
- [x] `package.json`を`0.2.0`へ更新する。pnpm lockfileはproject version fieldを持たないため変更なし。
- [x] staleな機能数、test数、対応file type、未実装表記を現行ドキュメントから除く。archive／一次資料の歴史的記述は保持する。
- [x] `pnpm check`、`pnpm build`、`git diff --check`を通す。62 tests green。
- [x] `npm pack --dry-run`相当で配布物一覧を確認し、秘密、fixture、RAG、research script、stateが混入しないことを確認する。
- [x] tarballを隔離prefixへinstallし、CLI version、doctor、MCP 6 toolsをsmokeする。
- [x] 対象pathだけをstageし、stage内容、秘密scan、cached diffを再確認する。
- [ ] 日本語commit messageでcommitする。
- [ ] 現在branchをGitHubへpushする。
- [ ] `gpt-connector@0.2.0`をnpmへ公開する。
- [ ] npm registryで`0.2.0`をread-backする。
- [ ] 公開版をglobal installし、CLI versionとdoctorを確認する。
- [ ] release結果を文書へ反映し、必要ならrelease記録commitを追加してpushする。

## 停止条件

- full gateがgreenでない。
- tarballに秘密、browser profile、state、fixture、RAG、research scriptが混入する。
- npm identityまたはregistryが想定外。
- GitHub remote／branchが想定外。
- npm上に`0.2.0`が既に存在し、local内容と同一と確認できない。

## rollback

- GitHub push後の問題は追加revert commitで戻す。履歴改変やforce pushはしない。
- npm versionは上書き不可。問題時はdeprecateし、修正版を次patchとして公開する。
- global installは`npm uninstall -g gpt-connector`または既知version再installで戻す。

## 実行中の失敗記録

- 初回`npm publish`は公開前の`prepublishOnly`で停止した。隔離tarballを展開した`.gpt-connector-tmp/**`をESLintが走査し、生成済みJS／d.tsへ42 errorsを報告した。
- npm registry read-backは`0.1.0`のみで、`0.2.0`未公開を確認した。
- 根本修正としてgitignore済みrelease作業directoryをESLint ignoreへ追加し、展開物を残したままfull gateを再実行する。
